kind = "web"
previewPath = "/"
title = "Legal Services"
version = "1.0.0"
id = "artifacts/legal-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20602

[services.development]
run = "pnpm --filter @workspace/legal-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/legal-web", "run", "build" ]
publicDir = "artifacts/legal-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20602"
BASE_PATH = "/"
